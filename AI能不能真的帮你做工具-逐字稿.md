# 《AI 能不能真的帮你做工具？现场拆一个真实需求》逐字稿

## 使用说明

这份逐字稿按 130 分钟线下分享准备，配套课件为 `AI能不能真的帮你做工具-现场拆解真实需求.html`。如果只有 90 分钟，优先保留第 1—15、24—25 页，删减第 17—23 页的现场细节。

主案例是本周实际推进的 `projects/orbit-game`：一个使用 Three.js 开发的 3D 太空街机游戏。案例中包含场景搭建、交互、规则、音效、战斗扩展、浏览器验证、单元测试和生产构建。分享时可以打开项目 README、`src/` 和 `design/verification.md` 作为证据。

讲述原则：明确区分“本周实际完成的内容”和“现场为了教学而设计的演示”。不要把一次成功结果包装成 AI 在所有项目里都能稳定复现的结论。

---

## 0. 开场｜0—5 分钟

对应第 1 页。

大家好，今天我们聊一个听起来很诱人、但也很容易被说得过头的问题：AI 能不能真的帮你做工具？

我这里说的“做工具”，不是让 AI 在聊天窗口里给我们一段代码，也不是让它生成一个看起来像产品的页面。我们今天要看的是更完整的一件事：从一个真实需求开始，经过需求拆解、代码开发、错误处理、测试和验证，最后得到一个别人能够运行、能够使用、也能够继续修改的东西。

这周我实际推进了一个项目：一个 3D 太空街机游戏，叫 ORBIT。它有飞船、星环、陨石、能量收集、护盾、加速、暂停、音效，后面还加入了敌机、激光、敌方弹幕和母舰 Boss。

我想用这个项目回答三个问题：第一，AI 到底能帮我们完成哪些开发工作；第二，为什么它有时能快速推进，有时却会把问题越改越复杂；第三，除了模型本身，我们还需要为它准备什么工程系统。

这里可以先问大家一个问题：如果让 AI 帮你做一个工具，你最担心的是不会写代码，还是做出来不能用？

今天最后我们会发现，这两个问题其实不是一回事。不会写代码，可能已经不是最大门槛；能不能把需求说清楚、把边界设好、把结果验证出来，往往更关键。

---

## 1. 本周案例｜5—10 分钟

对应第 2 页。

我们先把本周发生的事情摆出来。

第一阶段是需求和视觉。我们先确定这是一个什么游戏：玩家驾驶一艘飞船，穿过不断出现的星环，在三条航道之间切换，躲避陨石，收集绿色能量。视觉上是深青黑的太空背景、荧光色星环和低多边形飞船。

第二阶段是实现。项目里有几个相对清晰的模块：`scene.js` 负责 Three.js 场景、几何体、灯光和背景；`main.js` 负责游戏循环、输入、状态、音效和 HUD；`rules.js` 负责碰撞、积分和难度规则。

第三阶段是扩展。游戏从单纯的躲避和收集，扩展到了战斗模式：敌机、青色激光、红色敌弹、爆炸碎片和母舰 Boss。这个阶段很容易引入跨模块问题，所以单独放了 `combat.js`，并为战斗行为增加测试。

第四阶段是验证。我们没有只看“页面能不能打开”，而是验证了桌面键盘、手机触控、暂停和恢复、音效开关、结算和重新开始。项目记录里还保留了测试、构建和控制台检查结果。

这张表很重要，因为它告诉我们：AI 帮忙做工具，不等于它写完一个文件。真正的交付物是一组代码、规则、测试、文档和验证记录。

---

## 2. 一句需求为什么不够｜10—16 分钟

对应第 3 页。

如果我只对 AI 说一句：“帮我做一个太空游戏。”它当然可以开始写代码，但它不知道很多关键问题。

玩家是在一条航道还是三条航道？有没有加速？撞到陨石之后怎么办？分数怎么计算？手机能不能玩？暂停时分数还会不会增长？第一版要不要登录、排行榜和联机？

这些不是代码问题，而是产品和工程问题。如果我们不提前做决定，AI 就会替我们做决定，而且每次可能不一样。

所以我们把需求改写成四块。

第一块是体验目标：玩家驾驶飞船穿过星环，躲避陨石并收集能量。

第二块是核心循环：变道、加速、收集或碰撞、得分、继续或结算。

第三块是交互范围：桌面使用键盘，手机使用触控；暂停和重试要可用。

第四块是非目标：首版不做账号、不做后端、不做联机、不做支付，最高分先保存在本地。

我特别强调非目标，因为它是 AI 项目里非常有价值的一句话。AI 很擅长扩展，但真实开发需要经常收缩。你不写非目标，它就容易把一个小工具变成一个没有边界的系统。

这里可以给大家一个简单模板：谁在什么场景下，使用什么输入，得到什么结果；哪些事情明确不做；什么条件满足时算完成。

---

## 3. 任务卡｜16—21 分钟

对应第 4 页。

有了需求，还不能直接让 Agent 一次做完整个游戏。我们把它拆成一组任务卡。

第一张卡是场景基础：相机、光照、背景和星环。第二张是玩家循环：三条航道、变道、速度和暂停。第三张是规则模块：碰撞、积分、难度和护盾。第四张是反馈体验：HUD、音效、提示和结算。第五张是战斗扩展。最后才是测试、构建和浏览器验证。

为什么要拆？因为每一张卡都有清晰的完成标准。比如“实现三条航道”可以验证玩家能否从中间切到左边、从中间切到右边，不能继续越界；“实现暂停”可以验证分数是否停止增长，恢复后是否继续；“实现 Boss”可以验证生成、血量、弹幕和击破奖励。

一张任务卡最好包含四件事：目标、涉及文件、完成标准、验证命令。Agent 返回时也不要只说“完成了”，而是要说改了什么、怎么验证、还有什么风险。

这就像把一个模糊的项目愿望，变成了人和 Agent 之间的工作接口。

---

## 4. 裸模型对照｜21—26 分钟

对应第 5 页。

现在我们做一个对照：如果只有一个模型和一句 Prompt，会发生什么？

首先是上下文不足。它不知道当前项目用什么框架，不知道视觉参考是什么，也不知道已经有哪些文件。

第二是目标漂移。你本来只想做一个能玩的最小版本，它可能很快提出排行榜、账号、商店、多人模式。每个想法都不一定错，但它们会让核心体验无法收口。

第三是验证缺失。页面能打开，并不代表暂停正确、碰撞正确、手机可用。很多 AI 生成的项目，问题不是没有代码，而是没有人定义什么叫正确。

第四是错误难定位。如果所有逻辑都塞进一个文件，出现问题时你很难知道是规则、场景、状态还是 UI 造成的。

所以我不是说裸模型没用。裸模型很适合快速探索、生成起点和讨论方案。但它更像一个很会干活的实习生：如果你不给它项目背景、工作边界和验收方式，它很可能会用自己的理解把事情做下去。

---

## 5. Harness 是什么｜26—33 分钟

对应第 6 页。

这里进入今天的核心概念：Harness Engineering。

我会用四个词解释它。

第一个是 Context，也就是上下文。包括项目结构、README、设计规范、当前任务和之前已经做过的决定。它决定 Agent 看见什么。

第二个是 Constraints，也就是约束。包括哪些文件可以改、技术选型是什么、哪些功能不做、哪些操作需要确认、哪些数据不能碰。它决定 Agent 不能做什么。

第三个是 Tools，也就是工具。包括终端、测试、构建、浏览器、截图、日志和脚本。它决定 Agent 能做什么。

第四个是 Feedback，也就是反馈。包括报错、测试结果、视觉差异、分数、状态变化和人工验收。它决定 Agent 如何知道自己做得对不对。

所以 Harness 不是一个更长的 Prompt。它是一套让模型进入工作循环的工程系统。

可以把它理解为：模型提供潜在的推理和生成能力，Harness 负责把这种能力接到真实任务上。没有上下文，它不知道方向；没有约束，它容易越界；没有工具，它不能行动；没有反馈，它只能猜。

---

## 6. 仓库作为上下文｜33—39 分钟

对应第 7 页。

我们看本周项目的结构。

`README.md` 解决的是如何运行、怎么操作和项目有哪些组成。`scene.js`、`main.js`、`rules.js`、`combat.js` 让不同的责任有不同的落点。测试文件告诉 Agent 哪些行为已经被当作契约。`design/visual-spec.md` 记录视觉目标，`design/verification.md` 记录实际检查结果。

这些文件不是为了让项目显得专业，而是为了让下一个人或下一个 Agent 能够接手。

当 Agent 要修改 Boss 行为时，它可以找到 `combat.js` 和相关测试；当它要改移动规则时，可以找到 `rules.js`；当它要判断手机是否合格，可以读验证记录和运行浏览器检查。

这就是我说的“仓库作为接口”。接口不只存在于 API 里，也存在于目录、命名、文档、测试和命令中。

一个很实用的问题是：如果明天换一个人来接手，这个人能不能回答三件事——我现在在哪、我要改什么、怎样证明改对了？如果不能，说明 Harness 还不完整。

---

## 7. 约束与权限｜39—44 分钟

对应第 8 页。

很多人说 AI 编程时，会强调“让它拥有更多权限”。但真正成熟的做法是：只给它完成当前任务所需的权限。

在这个游戏项目里，首版不接后端、不接账号和联机，就是范围约束。无需 API 密钥、无需外部数据，就是环境约束。不自动发布、不覆盖未知文件，就是操作约束。手机不能横向溢出、暂停后状态要保持，就是体验约束。

这些约束降低了什么？降低了错误动作的代价。

如果换成会议纪要工具，自动生成一份草稿通常风险较低；但自动把内容发给客户、自动改 CRM、自动删除文件，风险就明显不同。能做不等于该做，能自动做也不等于应该自动做。

所以 Harness 工程一定要把权限、边界和人工确认纳入设计，而不是等系统出了事故再补。

---

## 8. 工具链｜44—50 分钟

对应第 9 页。

AI 只有写代码的能力，还不够完成真实开发。它需要一条工具链。

编辑器或 Agent 负责读取项目、规划任务和修改文件。`npm test` 负责验证规则，比如航道边界、跨帧碰撞、相邻航道安全、得分和难度上限。`npm run build` 负责确认生产构建能完成。浏览器负责模拟真实用户操作。截图和控制台日志则负责把视觉和运行状态带回来。

这些工具各自解决不同问题。

单元测试不能告诉我们手机按钮是否真的可点；浏览器试玩也不能替代规则测试。构建成功不能证明产品好用；产品截图好看也不能证明代码可维护。

因此“验证”不是一个动作，而是一组互相补位的观察方式。

如果课堂允许，可以现场打开项目的 `package.json`、测试文件和验证记录，让大家看到这些命令不是理论上的，而是实际项目中的交付环节。

---

## 9. 反馈回路｜50—55 分钟

对应第 10 页。

AI 开发的关键循环可以概括成五步：读取事实、提出改动、执行验证、读取反馈、最小修复。

第一步，先读当前文件和已有行为，不要直接猜。第二步，只完成一个明确任务。第三步，运行对应的测试、构建或浏览器检查。第四步，读取失败信息、截图、分数、护盾和控制台。第五步，定位原因，做最小修复，再回归。

这比“一次生成几千行代码”更稳定。

举一个手机适配的例子。我们不是说“手机体验不好，请优化一下”，而是观察到一个具体现象：手机页面出现横向溢出，边缘航道看不全。这个现象给出了线索，下一步就可以检查镜头范围、容器宽度、按钮布局和 CSS 溢出。

好的反馈应该能够缩小问题范围。模糊的“还是不对”，对人和 Agent 都没有帮助。

---

## 10. 真实失败｜55—62 分钟

对应第 11 页。

这一页是我认为最有价值的部分，因为真实开发不会只有第一次成功。

比如暂停后分数继续变化。现象是分数在暂停时还在增长，线索是游戏循环没有正确冻结，修复之后要重新验证暂停和恢复。

比如手机边缘航道看不全。现象不是“视觉差一点”，而是玩家无法看清完整操作范围，线索是手机镜头和场景范围不匹配，修复是拉远镜头，再做手机截图检查。

比如 Boss 弹幕难以躲避。问题不是 Boss 不能生成，而是生成规则没有保证始终留出安全航道。修复要同时调整规则和测试。

再比如生产构建出现体积警告。它不一定让页面立刻坏掉，但说明交付方式需要优化。最后把 Three.js 单独打包，重新构建并确认警告消失。

这就是 Harness 的价值：它把“出了问题”翻译成现象、原因、改动和回归证据。

请大家注意，不要让 Agent 盲目重试，直到它终于给出一个看起来不错的结果。每次失败都应该被记录、分类和验证。否则我们只是把错误从一个地方移动到了另一个地方。

---

## 11. 验证标准｜62—68 分钟

对应第 12 页。

我们如何证明这个游戏真的能用？

第一层是规则验证：航道边界、跨帧碰撞、相邻航道安全、积分和难度上限。

第二层是战斗验证：敌机能否被击毁，Boss 能否生成和击破，敌弹是否造成伤害，重置后状态是否恢复。

第三层是设备验证：桌面键盘、手机触控、暂停、音效和结算流程。

第四层是交付验证：生产构建通过，控制台没有 error 或 warn，手机没有横向溢出。

本周的记录里，9 项测试通过，生产构建通过，桌面和手机交互也进行了核对。

这里的重点不是“9”这个数字本身，而是验证标准被提前写出来了。没有标准，就只能凭感觉说“应该可以”；有了标准，我们才知道下一步是继续开发、修复问题，还是可以交付。

同样的方法可以迁移到业务工具：输入一份会议记录，检查行动项有没有负责人和截止日期；输入缺失日期的记录，检查系统会不会诚实提示，而不是编造；导出文件，再检查格式和编码。

---

## 12. 从游戏到业务工具｜68—74 分钟

对应第 13 页。

有人可能会说，游戏和业务工具不一样。确实，场景不同，但工程问题高度相似。

游戏里的航道和碰撞，对应业务工具里的输入边界和异常数据。游戏里的 HUD 和结算，对应业务工具里的状态、提示和最终交付。手机触控对应不同设备和运行环境。Boss 和弹幕对应复杂流程和高风险动作。本地最高分对应历史记录、可追踪性和回归数据。

我们本周还做了 Skills 专题课和会议行动项实操包。那里同样有输入、规则、输出格式、边界测试和参考答案。

所以不管我们是在做游戏、会议纪要工具、内容整理工具，还是内部知识库，核心问题都是：Agent 看到什么？可以做什么？怎么知道做对了？出了问题能不能恢复？

这就是 Harness 方法可以迁移的原因。

---

## 13. Skill 与 Harness｜74—79 分钟

对应第 14 页。

这里顺便区分几个容易混淆的概念。

Skill 是方法包。它可以封装一类工作的流程、判断、资料和输出格式。比如 `meeting-actions` 这个 Skill，告诉 Agent 如何从会议记录中提取行动项，如何处理缺失信息，怎样输出固定格式。

Harness 是让这些方法稳定执行的系统。它提供项目上下文、工具、测试、权限、反馈和回滚。

简单说，Skill 告诉 Agent 怎么做；Harness 让它有条件做、做完能验收。

一个 Skill 的 `SKILL.md` 是方法，`references` 是依据，模板是输出契约，测试样例是行为验证。而项目目录、执行命令、权限和反馈回路，属于更大的 Harness。

两者都应该遵守几个原则：按需读取、最小权限、真实样例、可追溯输出。不要把所有知识都塞进一个文件，也不要因为有了 Skill 就认为结果一定可靠。

---

## 14. 人与 AI 的分工｜79—83 分钟

对应第 15 页。

Harness 不是把人从系统里拿掉。它是把人的时间从低价值执行，转移到关键判断。

AI 适合搭建结构、补全代码、编写测试、定位报错、重复重构和生成初版。

人必须决定需求价值、优先级、非目标、体验取舍和什么算完成。

人必须确认发布、外部写入、敏感数据、不可逆操作和业务承诺。

所以我更愿意把这种协作称为“人掌舵，Agent 持续推进”，而不是“无人驾驶”。

模型越强，能够自动推进的距离越长；但这不意味着责任边界消失了。相反，系统越接近真实业务，人工确认、权限控制和审计记录就越重要。

---

## 15. 从 Demo 到工具｜83—87 分钟

对应第 16 页。

一个 Demo 到底什么时候算工具？我通常会问四个问题。

第一，能复现吗？另一个人能不能按照 README 启动，而不是依赖原作者的聊天记录。

第二，能验证吗？关键路径有没有测试、样例或浏览器检查，而不是只看一张最终截图。

第三，能回滚吗？改动有没有边界和版本，失败后能不能回到上一个可用状态。

第四，能继续改吗？代码结构、文档和任务拆分，能不能让下一个 Agent 或人接手。

在本周的项目里，README 对应可复现，tests 和 verification 对应可验证，版本控制对应可回滚，模块结构和设计记录对应可继续修改。

如果这四个问题答不上来，项目通常还停留在一次性演示，而不是一个真正的工具。

---

## 16. 什么需求值得做｜87—91 分钟

对应第 17 页。

在开始开发之前，我们还需要判断：这件事是不是值得投入。

我会看三个信号。第一，它是不是重复出现？如果一件事只做一次，做工具的收益可能很低。第二，结果能不能描述？如果大家只能说“感觉更好”，就很难让 Agent 验证。第三，风险能不能控制？如果第一版必须接生产系统、碰敏感数据、影响真实权益，就不适合拿来做第一次试点。

所以最适合 AI 开发的第一件事，不是最宏大的产品，而是最容易形成反馈的工作。游戏案例也是一样：先做本地运行、核心循环和可验证交互，再考虑更复杂的模式。

---

## 17. 模块边界｜91—95 分钟

对应第 19 页。

模块化不是为了让代码看起来漂亮，而是为了让 Agent 改得动。

`scene.js` 负责场景、几何体、灯光和背景；`main.js` 负责循环、输入、状态和 HUD；`rules.js` 负责碰撞、积分和难度；`combat.js` 负责敌机、激光、敌弹和 Boss。

这样当暂停出了问题，我们可以先检查循环和状态；当碰撞出了问题，可以先检查规则；当 Boss 出问题，可以先检查战斗模块。我们不需要把整个项目重新生成一遍。

清晰的职责边界，就是给 Agent 的可操作地图。边界越清楚，改动范围越小，失败后的归因越容易。

---

## 19. 状态模型｜99—103 分钟

对应第 20 页。

很多产品需求使用的是体验语言，比如“手感好”“暂停自然”“结算清楚”。这些话对人有意义，但对验证不够具体。

我们可以把它们翻译成状态和事件。游戏状态从 READY 到 PLAYING，可以进入 PAUSED，也可能进入 GAME_OVER。PLAYING 接受变道、加速、碰撞和计分；PAUSED 停止时间推进，但保留画面和当前状态；GAME_OVER 停止输入，显示结算，并允许重置。

这样一来，暂停就不再是一个模糊的体验要求，而是一个可以检查的状态转移：暂停时分数不增长，护盾不变化，恢复后继续。

状态模型既帮助 AI 写代码，也帮助测试覆盖边界。

---

## 20. 浏览器验收｜103—108 分钟

对应第 21 页。

代码和测试通过之后，我们还要用用户的方式走一遍界面。

先点击开始，检查 HUD 是否显示初始分数、速度和三个护盾。然后左右变道、加速、收集能量。接着撞一次陨石，看护盾是否减少、反馈是否出现。再用按钮或空格暂停，观察分数是否保持不变。最后故意让护盾耗尽，检查结算信息和重新出发后的状态。

如果条件允许，还要在手机上做同样的操作，检查触控按钮、镜头范围和横向溢出。

这类验证非常重要，因为代码里存在一个按钮，不代表用户真的能按到；测试通过，也不代表手机上布局没有问题。

---

## 21. 测试证据｜108—112 分钟

对应第 22 页。

验证还要诚实说明自己的范围。

`rules.test.js` 可以证明航道边界、碰撞、得分和难度规则；但它不能单独证明手机按钮是否可用。`combat.test.js` 可以证明敌机、Boss、敌弹和重置行为；但它不能证明画面构图合格。`npm run build` 可以证明生产构建完成；但不能证明真实用户喜欢这个产品。浏览器核对可以证明关键操作路径，但也不代表所有输入组合都没有问题。

本周记录里有 9 项测试通过、生产构建通过和桌面手机交互核对。我们应该说“这些范围内有证据”，而不是说“产品绝对没有问题”。

这种对证据边界的诚实，是 Harness 工程的重要部分。

---

## 22. 功能扩展｜112—116 分钟

对应第 23 页。

为什么后面还能加敌机和 Boss？因为基础 Harness 没有被破坏。

战斗模式复用了原有场景和几何体资源，减少重复实现；敌机、激光、敌弹、爆炸和 Boss 的逻辑放进 `combat.js`，没有把主循环无限膨胀；击毁、受伤、得分和重置通过清晰的状态变化连接；新增行为同时补测试、试玩和结算检查。

扩展不是简单堆功能，而是沿着已有的模块、契约和反馈回路增加能力。

真正的可扩展性来自边界、契约和回归，不来自代码数量。Agent 越能理解系统边界，越能在已有能力上继续推进，而不是每次从头重写。

---

## 23. 两周试点｜116—121 分钟

对应第 24 页。

把这套方法带回业务时，不需要一开始做完整产品。可以用两周做一个小试点。

第 1 到第 2 天，选任务，写输入、输出、非目标和验收标准。第 3 到第 5 天，整理项目上下文，准备真实样例和边界样例。第一周结束时完成最小实现，跑自动检查和人工验收。第二周连续使用，记录耗时、返工、遗漏、确认次数和成本。结束时保留有效规则，删除临时做法，再决定是否扩大范围。

会议行动项、课程内容整理、日报生成、资料归档，都可以作为低风险试点。

不要只看模型回答是否漂亮，或者 Demo 是否一次成功。用前后基线和真实样例，决定是否值得继续投入。

---

## 24. 收束前的设计卡｜121—125 分钟

对应第 25 页。

现在把今天的内容压缩成一张 Harness 设计卡。

先写需求：谁在什么场景下要解决什么问题？再写输入：有哪些材料，缺什么信息就必须停下来？然后写输出：用户拿到什么，什么格式方便继续工作？

接着写上下文：Agent 必须知道哪些项目事实？写约束：哪些文件、数据和动作不能碰？写验证：用什么命令、样例、设备和人工标准验收？最后写回滚：失败之后如何比较、恢复和重新开始？

这张卡的价值是让一次分享变成一个可以执行的下一步。它不要求你立刻做大产品，但要求你先把问题和边界说清楚。

---

## 25. 收束｜125—130 分钟

对应第 17 页。

今天我们没有证明“AI 可以替代开发团队”，也没有证明“任何人都能一键做出复杂产品”。我们看到的是一个更有用、也更诚实的结论：AI 已经可以参与真实工具开发，但它需要被放进一个好的工程闭环里。

这个闭环包括五件事：可读的上下文、明确的约束、可执行的工具、及时的反馈和可靠的验证。

从一个高频、低风险、边界清楚的需求开始，把它写成任务卡；建立项目结构和验收命令；让 Agent 小步推进；保留失败记录；最后用真实样例验证。

所以不要先问：“AI 能不能替我做完所有事？”

先问：“哪一段重复工作，值得被工程化？”

请大家现在写下一个自己的候选需求，并补齐四句话：输入是什么，输出是什么，如何验收，哪些事情不能自动做。

如果这四句话写不出来，先不要开发，继续澄清需求。如果写得出来，就可以从一个两周小试点开始。

谢谢大家。

---

## 备用：现场演示顺序

如果现场需要实际打开项目，可以按这个顺序：

1. 打开 `projects/orbit-game/README.md`，展示运行方式、操作方式和项目结构。
2. 打开 `projects/orbit-game/src/rules.js` 与对应测试，说明规则与验证如何分离。
3. 打开 `projects/orbit-game/src/combat.js`，展示战斗扩展如何独立出来。
4. 打开 `projects/orbit-game/design/verification.md`，展示桌面、手机、暂停、结算、音效和构建记录。
5. 运行 `npm test`，说明测试证明什么、不证明什么。
6. 运行 `npm run build`，说明生产构建是交付检查，不是功能测试的替代品。
7. 在浏览器中试玩：变道、加速、收集、暂停、重试；如果条件允许，再展示手机触控。

## 备用：现场失误处理

| 现场现象 | 讲师处理 |
| --- | --- |
| 项目启动失败 | 先看 README 和终端错误，不要假装成功；可切换到代码与验证记录讲解 |
| 浏览器无法访问本地项目 | 明确说明环境限制，展示已有截图和验证记录，不把未验证内容说成已验证 |
| 测试失败 | 把它当成案例，先读失败信息，再决定是代码、需求还是测试的问题 |
| AI 生成结果与预期不同 | 回到任务卡和验收标准，检查上下文是否清楚，不要只继续追加 Prompt |
| 现场时间不足 | 保留“需求 → Harness → 验证 → 收束”四段，删掉扩展功能细节 |
