# 《AI 工具现场拆解：Harness 工程实战》逐字稿

## 使用说明

这份口播稿对应 `AI工具现场拆解-harness工程实战.html`，按 25 页、约 110—130 分钟准备。它与《AI 能不能真的帮你做工具？现场拆一个真实需求》的稿子是两套不同的讲述重点：前者讲 Harness 这套工程系统，后者讲从需求到工具的完整案例。

本稿的示例主要使用会议行动项、内容整理和小游戏开发三个场景。没有实际执行的演示要说成“现场计划”，已经有文件、测试或验证记录支持的内容才说成事实。

---

## 01. 开场｜0—5 分钟

大家好。今天我们讨论一个经常被忽略的问题：为什么同样一个模型，有的人用它可以持续做出工具，有的人只能得到一段看起来不错的代码？

今天的答案不是再学一个 Prompt，而是理解 Harness Engineering。它关心的是，如何给 Agent 准备上下文、约束、工具和反馈，让它能够持续推进一项真实任务。

我们会用几个真实工作场景来讲：会议记录转行动清单、课程资料整理，以及一个 Three.js 太空游戏。重点不是展示某个模型多聪明，而是拆开“让它完成工作”所需要的工程系统。

---

## 02. 先定义问题｜5—9 分钟

很多人一上来就说：“帮我做个网页。”这句话对人都不够清楚，对 Agent 更不够清楚。

我们需要先回答四个问题：谁在用？输入是什么？输出是什么？什么条件算完成？比如会议行动清单，使用者是项目负责人，输入是会议记录，输出是可编辑的任务列表，完成标准是负责人、截止时间和依据都能被检查。

这一步的价值，是把“做出来”变成“验收得到”。只要结果不能描述，后面就很难设计测试、工具和反馈。

---

## 03. 真实需求｜9—14 分钟

现在看一个具体需求：把会议纪要变成行动清单。

首版只做四件事：读取会议记录，识别行动项，输出可编辑任务表，支持 Markdown 导出。它不接所有会议平台，不自动发消息，也不替人猜负责人。

这个案例很适合讲 Harness，因为它同时有事实依据、结构化输出、缺失信息、冲突信息和外部副作用。我们可以清楚地判断“识别正确了吗”，也可以明确哪些动作必须停在人工确认之前。

---

## 04. 需求到任务卡｜14—19 分钟

大需求要拆成小任务。第一张卡创建最小页面和输入区；第二张定义任务数据结构；第三张实现解析和编辑表格；第四张增加导出和错误提示；第五张跑测试并修复回归。

每张任务卡都需要目标、涉及文件、完成标准和验证命令。比如“实现可编辑表格并通过三条测试”，比“做一个好用工具”更适合作为 Agent 任务。

任务卡的本质，是把人和 Agent 之间的合作变成一个清晰接口。Agent 不需要猜下一步，人也不需要重新解释整个项目。

---

## 05. Harness 定义｜19—25 分钟

Harness 不是更长的 Prompt。它至少有四个部分。

Context 是上下文：项目结构、文档、数据模型、当前任务。Constraints 是约束：哪些文件可以改，哪些动作不能自动执行。Tools 是工具：终端、测试、构建、浏览器和脚本。Feedback 是反馈：报错、测试结果、截图、运行状态和人工验收。

如果只有上下文没有反馈，Agent 会很有自信地犯错；只有工具没有边界，它可能改得太多；只有反馈没有上下文，它很难知道应该改哪里。

所以 Harness 的目标不是让模型变聪明，而是让正确的下一步更容易发生。

---

## 06. 仓库作为接口｜25—30 分钟

一个好的仓库，本身就是给 Agent 的说明书。

README 告诉它如何启动、如何操作、如何验收。规则文件告诉它目录职责、命名方式和不能做什么。测试文件记录已经承诺的行为。设计和决策文档记录产品目标和取舍。验证记录说明哪些环境已经实际检查过。

请大家记住三个问题：我现在在哪？我要改什么？怎样证明改对了？如果仓库不能帮助 Agent 回答这三个问题，最好的做法通常不是继续补 Prompt，而是补项目上下文。

---

## 07. 约束与权限｜30—35 分钟

“不要乱改”不是一个足够好的约束。好的约束应该尽量变成机器可以检查的边界。

例如允许修改的目录、生成文件的固定位置、命令的确认规则、敏感数据的排除范围，以及正式发布前的人工门。

低风险工具也需要边界。可以自动生成会议草稿，但不能自动发送给客户；可以运行本地测试，但不能因为测试通过就自动发布生产环境。能力越强，权限边界越要清楚。

---

## 08. 验证系统｜35—41 分钟

没有验证，AI 只是“看起来完成了”。

我们可以为会议行动项准备正常样例、缺少日期的样例、冲突截止时间的样例、缺少负责人的样例，以及重复执行的样例。

测试不只测成功路径。尤其要检查模型会不会在缺少依据时擅自补齐，会不会把目标写成承诺，会不会把“核实退款条件”写成“退款已经批准”。

验证的价值是告诉 Agent 下一步该修什么，而不是给它一个漂亮分数。

---

## 09. 反馈回路｜41—46 分钟

Agent 的核心循环是：读取、改动、运行、观察、修正。

每次先读取当前项目事实，只改一个相对独立的能力，然后执行测试、构建或浏览器检查。接着读取具体反馈，再做最小修复。

“页面不对”不是好的反馈；“手机宽度 390 像素时出现横向溢出”才是好的反馈。“测试失败”也不够；需要知道是哪个输入、哪个断言、哪个文件和哪一个行为失败。

反馈越具体，Agent 越像在解决问题，而不是在继续猜。

---

## 10. 现场开发与失败｜46—52 分钟

现场最好保留一次可控失败。比如日期解析没有覆盖某种输入格式，任务重复出现，导出文件乱码，或者页面能打开但不能完成关键流程。

遇到失败时不要马上让 Agent 重试。先把现象、可能原因、下一步动作写出来，再只修改一个变量，重新运行验证。

失败的价值在于暴露 Harness 的缺口：可能是上下文没写清，可能是测试没覆盖，可能是工具输出太模糊，也可能是需求本身没有做决定。

---

## 11. 人与 AI 的分工｜52—57 分钟

AI 适合搭页面、补样板代码、写测试、定位报错和重构重复逻辑。

人要决定需求价值、优先级、非目标、数据口径和什么算完成。人还必须确认正式发布、外部写入、敏感数据、不可逆操作和业务承诺。

所以成熟的协作不是无人驾驶，而是人掌舵、Agent 持续推进。人不需要逐行指挥代码，但必须把关方向、边界和结果。

---

## 12. 从 Demo 到工具｜57—62 分钟

一个 Demo 要成为工具，至少要过四关：可复现、可观察、可回滚、可迭代。

另一个人能否按 README 启动？出了问题能否看到日志和错误？改坏之后能否恢复？下一个人或 Agent 能否继续修改？

如果这些问题答不上来，项目可能依然很有展示效果，但还没有成为可靠的工作资产。

---

## 13. 需求筛选｜62—66 分钟

不是所有需求都适合立刻做成工具。

优先选择重复出现、结果可描述、风险可控制的任务。一次性的创意、无法定义完成标准的任务、必须立刻接入生产系统的高风险任务，都不适合作为第一次 Harness 实践。

好的起点不是最宏大的产品，而是最容易形成反馈的工作。

---

## 14. 裸模型对照｜66—71 分钟

如果只给一句 Prompt，常见问题有四类：上下文不足、目标漂移、反馈缺失、错误难定位。

这不是说裸模型没有价值。它可以快速探索方案、生成起点、解释代码。但如果要交付一个持续运行的工具，就必须把模型放进更完整的工程系统。

对照的目的不是比较谁更聪明，而是让大家看到模型能力和系统能力的区别。

---

## 15. 上下文编译｜71—76 分钟

Agent 每次行动前，都需要一份有效上下文。它不是把全部聊天历史塞进去，而是根据当前目标，选择相关文件、测试、工具结果、权限和之前的决策。

可以把它理解成上下文编译：把复杂项目状态编译成当前这一步真正需要的信息。

上下文太少，Agent 会误解项目；上下文太多，重要信息会被淹没。好的 Harness 需要管理上下文的新鲜度、相关性和优先级。

---

## 16. 工具设计｜76—81 分钟

工具越多不一定越强。一个好工具需要输入契约、输出契约、失败契约和恢复方式。

输入要知道必填字段和类型；输出要结构化；失败要能区分输入错误、环境错误和业务错误；恢复要支持重试、取消、回滚或转人工。

工具设计的目标不是让 Agent 做更多事，而是让它少误判。尤其在外部写入和敏感操作上，明确的失败方式比“尽量成功”更重要。

---

## 17. 概念分工｜81—86 分钟

Prompt 是本次任务的即时要求。Skill 是可复用的方法。Tool 是可调用的能力。Memory 是跨任务保存的事实或经验。Harness 则负责把它们组织成可执行、可验证的闭环。

当结果不稳定时，不要默认继续加 Prompt。先判断问题发生在哪一层：方法不清，工具不可用，上下文过期，还是验证标准缺失。

概念分工清楚，系统才知道该改哪一层。

---

## 18. 任务协议｜86—90 分钟

我建议让 Agent 每次都用“改动、证据、风险”的格式交付。

它要说明完成了什么，改了哪些文件，执行了哪些命令，哪些部分没有验证，还有什么风险需要人确认。

这比一句“已经完成”有用得多，因为人可以快速审查，也可以把这份报告交给下一步 Agent 继续工作。

---

## 19. 评估集｜90—95 分钟

没有评估集，就没有可靠迭代。

准备正常、缺失、冲突、边界和重复执行样例。每次修改 Prompt、Skill、工具或 Harness 时，都在固定基线和未参与调试的样例上比较。

评估集不是为了追求一个漂亮分数，而是为了回答：这次修改到底让系统变好了，还是只让它记住了几个示例？

---

## 20. 观察与日志｜95—99 分钟

如果没有轨迹，很难知道 Agent 为什么失败。

可以记录任务 ID、步骤、工具调用、耗时、错误、重试、最终状态和人工介入。然后按失败类型聚类，决定下一步应该补规则、补测试、改工具还是增加人工门。

日志不是为了证明 Agent 很忙，而是为了找到最值得修的地方。也不要无边界收集数据，只保留真正帮助定位和改进的最小信息。

---

## 21. 预算与循环｜99—103 分钟

给 Agent 自主性，也要给它时间、调用和权限预算。

超过时间就暂停并报告状态；超过重试次数就停止；任务完成后收回不再需要的权限；高风险动作进入人工确认。

自主性不是无限放权，而是在明确预算内持续推进。预算让失败的代价可控，也让成本可以被管理。

---

## 22. 自我改进｜103—108 分钟

Harness 可以根据失败提出改进，但“让系统自己改自己”不等于它会自动变好。

正确的流程是观察失败、提出最小修改、在基线和 holdout 上回归、人工检查副作用，然后接受或回滚。

尤其要注意，模型自评不能替代外部验证。只要修改涉及权限、工具或系统行为，就必须保留版本、证据和恢复路径。

---

## 23. 团队落地｜108—112 分钟

个人技巧变成团队能力之后，还要回答四个问题：谁维护？怎么版本化？谁有权限？使用者如何反馈？

一个 Skill 或 Harness 一旦被多人依赖，就不再只是某个人的 Prompt。它需要维护人、变更记录、回归样例和反馈渠道。

工程化的最后一公里，往往不是再加一个模型，而是把责任和治理补齐。

---

## 24. 练习｜112—118 分钟

现在请大家写一张自己的 Harness 设计卡。

写清需求、输入、输出、上下文、约束、验证和回滚。可以用会议行动项、内容整理、日报生成或资料归档作为练习题。

如果某一项写不出来，不要急着开发。比如说不清缺什么信息就必须停下，说明输入边界还没有定义；说不清怎么验收，说明结果还没有变成可观察条件。

---

## 25. 收束｜118—125 分钟

今天的核心结论是：模型提供潜在能力，Harness 决定这些能力能否在真实任务里被调用、检查、修正和复用。

请记住六个关键词：需求拆解、任务协议、上下文、工具、评估、回滚。

回去以后，不要先做万能助手。选一个高频、低风险、可验收的小任务，准备一组真实样例，建立最小反馈回路，再用两周数据决定是否扩大范围。

不要只问模型有多强。要问：我们有没有把它放进一个能工作的系统？

